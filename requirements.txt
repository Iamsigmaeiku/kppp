influxdb-client[async]>=1.40
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0
numpy>=1.24

# Auth / DB / templates (Phase 2-3)
sqlalchemy>=2.0
aiosqlite>=0.20
alembic>=1.14
authlib>=1.3
itsdangerous>=2.2
python-multipart>=0.0.9
jinja2>=3.1
pillow>=10.0

# AI coach (Phase 4)
httpx>=0.27

# IANA timezone database — Windows has no system tz database, so
# zoneinfo.ZoneInfo() needs this package to resolve keys like "Asia/Taipei".
tzdata>=2024.1; sys_platform == "win32"

# Dev / test only
pytest>=8.0
pytest-asyncio>=0.25
